[Unit]
Description=Hail Jupyter Notebook

[Service]
ExecStart=/bin/bash -l -c 'exec "$@"' _ {{ anaconda_install_dir }}/anaconda3/bin/jupiter notebook --config={{ hail_install_dir }}/jupiter/conf/service.py
User={{ hail_install_owner }}

[Install]
WantedBy=multi-user.target
